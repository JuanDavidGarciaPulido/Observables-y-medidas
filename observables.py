#import numpy as np
#import Complex_Vectors as *

def p4_3_1(matriz):
    # valores, vectores = np.linalg.eig(matriz)
    return[0, 1]


def p4_3_2(matriz):
    # valores, vectores = np.linalg.eig(matriz)
    # prob = "h/2 *", valores
    return[0.5, 1]


def p4_4_1(matriz, matriz2):
    # esunitaria = es_unitario(matriz)
    # esunitaria2 = es_unitario(matriz2)
    # produco = productomam(matriz, matriz2)
    return True


def p4_4_2(matriz, vector, click):
    for i in range(click):
        # producto = productovam(matriz,vector)
        # proba = probabilidad(producto,vector)
        return [0.3, 0.7, 0.5, 0.5]